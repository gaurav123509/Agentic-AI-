{
  "name": "mailpilot-ai-backend",
  "version": "1.0.0",
  "description": "Production-ready backend for the MailPilot AI email triage platform.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --legacy-watch --watch server.js --watch config --watch controllers --watch middleware --watch models --watch routes --watch services --watch utils server.js"
  },
  "engines": {
    "node": "20.x"
  },
  "keywords": [
    "mailpilot-ai",
    "nodejs",
    "express",
    "mongodb",
    "openai",
    "email-triage"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "mongoose": "^9.4.1",
    "openai": "^6.33.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
