{
  "name": "mailpilot-ai",
  "private": true,
  "version": "1.0.0",
  "description": "MailPilot AI monorepo wrapper for deployment platforms.",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "start": "node backend/server.js",
    "dev": "npm --prefix backend run dev",
    "backend:start": "npm --prefix backend start",
    "backend:dev": "npm --prefix backend run dev"
  },
  "engines": {
    "node": "20.x"
  }
}
